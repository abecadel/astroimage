version: '3'

services:
  jupyter:
    build: .
    command: jupyter notebook --ip=0.0.0.0 --allow-root --no-browser --NotebookApp.token=''
    volumes:
      - ./notebooks:/app/notebooks
    ports:
      - "8888:8888"
